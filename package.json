{
  "name": "terra-design-system",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "npm run dev -w apps/web-catalog",
    "build:web": "npm run build -w apps/web-catalog"
  }
}
